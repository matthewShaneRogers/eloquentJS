{
  "name": "eloquentjs-tdd-mob",
  "version": "1.0.0",
  "description": "JavaScript TDD mob programming workspace using Jasmine — working through Eloquent JavaScript",
  "scripts": {
    "test": "jasmine",
    "test:watch": "jasmine --filter=''",
    "watch": "nodemon --exec 'jasmine' --watch src --watch spec --ext js"
  },
  "devDependencies": {
    "jasmine": "^5.4.0",
    "nodemon": "^3.1.0"
  }
}
